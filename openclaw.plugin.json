{
  "id": "tracevane",
  "name": "Tracevane",
  "description": "Tracevane 本地 AI Agent 控制工作台，聚焦 Gateway、Channel Connectors、CLI Agent 和运行态自愈。",
  "version": "0.2.3",
  "homepage": "https://github.com/binbin/tracevane",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "tracevane_delivery"
    ]
  },
  "skills": [
    "./skills"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "apiPort": {
        "type": "number",
        "default": 3760,
        "description": "Tracevane API 服务端口"
      },
      "autoStart": {
        "type": "boolean",
        "default": true,
        "description": "是否自动启动 Tracevane API 服务"
      },
      "transport": {
        "type": "object",
        "description": "Tracevane 暴露模式配置。默认同时保留 standalone 和 gateway 单口入口；只有显式关闭时才禁用对应入口。",
        "properties": {
          "preferredMode": {
            "type": "string",
            "enum": [
              "standalone",
              "gateway"
            ],
            "default": "standalone",
            "description": "Tracevane UI 优先展示和升级时继承的主入口模式"
          },
          "standalone": {
            "type": "object",
            "properties": {
              "enabled": {
                "type": "boolean",
                "default": true,
                "description": "是否启用 3760 独立 HTTP/WS 暴露"
              },
              "port": {
                "type": "number",
                "default": 3760,
                "description": "standalone 模式使用的 Tracevane 端口"
              }
            }
          },
          "gateway": {
            "type": "object",
            "properties": {
              "enabled": {
                "type": "boolean",
                "default": true,
                "description": "是否启用 OpenClaw Gateway 单端口模式；默认开启，显式设为 false 才禁用"
              },
              "basePath": {
                "type": "string",
                "default": "/tracevane",
                "description": "gateway 模式下的 Tracevane 基础路径"
              }
            }
          }
        }
      }
    }
  },
  "uiHints": {
    "__root__": {
      "label": "基础运行设置",
      "help": "优先调整 Tracevane 的启动和暴露方式；更细的链路配置在高级字段内。",
      "order": -10
    },
    "apiPort": {
      "label": "API 端口",
      "help": "Tracevane standalone HTTP / WS 默认端口。",
      "placeholder": "3760",
      "order": 10,
      "group": "__root__"
    },
    "autoStart": {
      "label": "自动启动 API",
      "help": "启用后宿主加载插件时会自动拉起 Tracevane API 服务。",
      "order": 20,
      "group": "__root__"
    },
    "transport": {
      "label": "暴露模式",
      "help": "管理 standalone 和 gateway 两种入口。",
      "order": 40
    },
    "transport.preferredMode": {
      "label": "首选入口",
      "help": "影响系统摘要展示和一键升级默认模式；gateway 模式仍会保留 3760 standalone 回退入口。",
      "placeholder": "standalone",
      "order": 5
    },
    "transport.standalone.enabled": {
      "label": "启用 Standalone",
      "group": "transport-standalone",
      "groupLabel": "Standalone",
      "groupHelp": "独立端口暴露，适合本地开发和直连调试。",
      "groupOrder": 10,
      "order": 10
    },
    "transport.standalone.port": {
      "label": "Standalone 端口",
      "placeholder": "3760",
      "group": "transport-standalone",
      "groupLabel": "Standalone",
      "groupOrder": 10,
      "order": 20
    },
    "transport.gateway.enabled": {
      "label": "启用 Gateway 模式",
      "group": "transport-gateway",
      "groupLabel": "Gateway",
      "groupHelp": "复用 OpenClaw Gateway 单端口入口。",
      "groupOrder": 20,
      "order": 10
    },
    "transport.gateway.basePath": {
      "label": "Gateway Base Path",
      "placeholder": "/tracevane",
      "group": "transport-gateway",
      "groupLabel": "Gateway",
      "groupOrder": 20,
      "order": 20
    }
  }
}
