/**
 * Compatibility shim: the canonical implementation lives in
 * `@/design/ui/skeleton`. Keep importing from here in existing feature code.
 */
export { Skeleton, SkeletonRow } from "@/design/ui/skeleton";
