/**
 * Compatibility shim: the canonical implementation lives in
 * `@/design/ui/state`. Keep importing from here in existing feature code.
 */
export { LoadingState } from "@/design/ui/state";
export type { StateViewProps as LoadingStateProps } from "@/design/ui/state";
