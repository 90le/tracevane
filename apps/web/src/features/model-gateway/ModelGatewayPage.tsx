/**
 * Placeholder for the Model Gateway domain. The real views and data layer are
 * built in later phases; this only establishes the route + heading.
 */
export function ModelGatewayPage() {
  return (
    <div className="grid gap-2">
      <h1 className="text-2xl font-semibold text-ink-strong">模型网关</h1>
      <p className="text-sm text-muted">模型网关的运行视图将在后续阶段接入。</p>
    </div>
  );
}
