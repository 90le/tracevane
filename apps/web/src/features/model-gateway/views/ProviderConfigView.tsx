import { EmptyState } from "@/shared/states/EmptyState";

import type { ModelGatewayViewProps } from "./types";

/**
 * Provider config subpage. Stubbed in Phase 6 Group A; filled by a later task.
 */
export function ProviderConfigView(_props: ModelGatewayViewProps) {
  return (
    <EmptyState
      title="建设中（本视图在后续任务实现）"
      description="Provider 配置子页面将在后续阶段接入。"
    />
  );
}
