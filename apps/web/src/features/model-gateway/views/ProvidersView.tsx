import { EmptyState } from "@/shared/states/EmptyState";

import type { ModelGatewayViewProps } from "./types";

/**
 * Provider list-detail view. Stubbed in Phase 6 Group A; filled by a later task.
 */
export function ProvidersView(_props: ModelGatewayViewProps) {
  return (
    <EmptyState
      title="建设中（本视图在后续任务实现）"
      description="Provider 列表与检视器将在后续阶段接入。"
    />
  );
}
