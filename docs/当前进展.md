# Tracevane Current Progress

> Status: active snapshot
> Updated: 2026-06-21
> Rule: this file keeps current state and open work only. Historical implementation detail belongs in git history.

## 1. Product State

The project has been reoriented from "OpenClaw management console" to "local-first AI Agent control workbench".

OpenClaw remains a supported runtime, but the durable Tracevane value is now:

- model gateway and provider protocol bridging
- CLI Agent workflow control
- workspace IDE for local edit/run/preview/verify loops
- IM/channel execution
- runtime recovery
- observability and evidence
- task-first human supervision

Product boundary reminder: OpenClaw is runtime substrate and platform support, not the center of the Tracevane product. New frontend work must be shaped around Tracevane's core jobs-to-be-done; OpenClaw-specific configuration, generic CRUD and host diagnostics stay under `/platforms/openclaw` or link out to official OpenClaw UI.

The broader product benchmark is now documented in `docs/产品对标策略.md`. Current product direction is to synthesize durable advantages from AI IDEs, app builders, observability/eval platforms, workflow automation and runtime managers without becoming a generic clone. New product design work should translate market lessons into one of the Tracevane pillars: Workspace IDE, Agent Runtime Control, Model Gateway, Channel Connectors, Evidence/Observability or System Guard/Recovery.

Tracevane is now the active product name and runtime id across package metadata, plugin id, UI copy, docs, generated client config defaults, release package names, installer script, gateway base path and `TRACEVANE_*` environment variables.

The old product naming compatibility layer has been intentionally removed from tracked code. Existing installs that still reference the previous plugin id, route prefix, provider id, custom media hint or environment variable names must be reinstalled or manually migrated to the Tracevane names.

The rename risk path has been hardened without restoring compatibility:

- `install-tracevane.sh` backs up retired extension directories before installing Tracevane.
- The installer prunes retired plugin ids, stale install records, stale load paths and stale plugin slot references from OpenClaw config, then writes only the Tracevane entry.
- Gateway service refresh now tries `openclaw gateway restart --safe` before falling back to plain restart, reducing the chance of interrupting active OpenClaw work.
- API/Web build and release packing clean generated output before compiling, so ignored `dist` residue cannot leak retired modules into a release tarball.

Dreaming / memory management and legacy plugin management are retired from Tracevane frontend/backend scope.

## 2. Stable or Near-Stable Tracks

### Model Gateway

- Provider Center, endpoint profiles, App Connections, model catalog, protocol conversion and usage summary are active.
- GLM two native endpoint protocols and Codex account Responses path have live proof in the current environment.
- Gateway usage page has been simplified to model request/token totals.
- Current priority is stability and protocol correctness, not feature expansion.

### Channel Connectors

- Feishu/Octo private chat to CLI Agent runtime is active.
- Codex, Claude Code and OpenCode are routed through structured persistent paths where available.
- Busy guard, file staging, permissions, progress, native compact and session evidence are covered by local and live smokes.
- TUI heartbeat parsing is only fallback evidence; official/structured events remain preferred.

### System Guard / Recovery

- OpenClaw 2026.6.8 config compatibility has been validated across web-write surfaces.
- Recovery can repair supported Gateway service and CLI package breakage on the current Linux/systemd-user environment.
- OpenClaw doctor warnings for plugin drift and obvious credential/config residue have been cleaned in the current environment.
- Remaining secret audit findings around agent model/auth plaintext keys are a separate migration risk, not an automatic repair path.

### Frontend Refocus

- **2026-06-22 重建决策**：判定 `apps/web-vue`（原型片段 raw 渲染 + page-mounts + 手写 hash 路由）功能不足、架构混乱、与 Aurora 偏离、工程基础薄弱，决定整体删除重建为 `apps/web`。新栈：React + Vite + Tailwind v4 + shadcn/ui 真正落地 + React Router 真路由 + TanStack Query 类型化数据层，feature-sliced 架构。采用大爆炸式：先迁移发布路径 → 删 `web-vue` → 脚手架 `apps/web` → **优先做满 Model Gateway** → 验证 → 其余域逐计划补齐。
- **2026-06-22 执行状态（分支 `frontend-rebuild`）**：重建已落地并通过浏览器验证。`apps/web-vue` 已删除（内容保留在 git 历史 `9b9041bd`），`apps/web` 已脚手架并实现：Aurora token + shadcn 组件 + 共享布局/三态；providers/typed API client/grouped 应用壳/HashRouter；Model Gateway 数据层（绑定全部现有后端路由，无臆造端点）；**Model Gateway 七个 view 全部完成**（overview/providers/providercfg/models/accounts/apps/usage，真实数据、确认+证据流、不伪造）。发布工具链（pack.sh/release-metadata）与系统测试套件已随新结构 green-up，全仓库无 `web-vue` 残留。修复了 `SidebarItem asChild` 把多个子节点塞进 Radix `Slot` 导致的首屏崩溃。验证：typecheck:api/web、build:api/web、`web-model-gateway`/`web-api-client` 合同测试通过、pack dry-run 产出含 `apps/web` 的 tarball；**浏览器 smoke 全绿**（`scripts/smoke-web-model-gateway.py`：7 个 view × 桌面/移动，0 console error、0 横向溢出）；dev 运行态前端 `http://127.0.0.1:5176` 渲染正常、后端 MG/health API 200。**其余域（dashboard/chat/ide/…）按后续计划逐个补齐。**
- **2026-06-22 已合并到 `main` + Model Gateway 全面优化完成**：重建分支已 fast-forward 合并到 `main`；侧边栏改为常驻全高/独立隐藏滚动条/可收缩。Model Gateway 在七视图基础上完成综合优化：App Connections 应用前 diff + 多版本回滚（回滚前 diff）+ 源文件编辑（强风险确认+备份+可回滚）+ 后端备份保留(20/app)与源编辑加固（大小上限/JSON 校验/TOML 保守校验/no-op 守卫）；Provider 新建协议预设选择器；模型目录搜索/窗口化/批量导入/批量能力/可选 detect 候选；ModelsView 搜索；补齐 4 项重建后缺失能力（网关密钥管理、运行诊断面板、守护服务状态+受控 restart、Provider 测试/smoke）；全局 UX 打磨。验证：typecheck/build(api+web)、后端 MG 测试 130/130、web 合同测试 11/11、浏览器 smoke 全绿。
- **其余域待办（次序/深度待用户确认）**：`/dashboard`(总览)、`/chat` `/ide` `/long-tasks` `/cli-agents`(运行)、`/im-channels` `/external`(连接)、`/files` `/approvals`(证据)、`/recovery` `/platforms`(系统) 共 11 个域当前为「建设中」占位。旧 `apps/web-vue` 中除 approvals 外均为真实（多为只读）页面，可从 git 历史(`9b9041bd`)作移植参考。每域按 Model Gateway 同款流程：数据层(消费现有 API)→视图(Aurora+shadcn)→三态→危险写入确认证据→typecheck/build/smoke 验证。
- **安装/部署边界（重要）**：插件已是**源码安装**（OpenClaw `load.paths` 指向本仓库目录，4 个服务从此运行）。`install-tracevane.sh` 是面向**已发布 release tarball** 的安装器——dry-run 证实它会下载 `tracevane.90le.cn/tracevane-<ver>.tar.gz` 并把本目录 `mv` 到备份、用已发布旧版替换，会**摧毁本次重建**，故不可对本源码目录运行。重建前端的"部署"应走：合并到 `main`（已完成）→ build → `pack.sh` 出新 tarball → 发布 → 再 `install-tracevane.sh` 下载新版；或本地直接重启 standalone/gateway 让其服务新构建。standalone `:3760` 入口由 OpenClaw 托管（无法在本会话安全识别/重启），生产入口刷新建议随发布流程或 `openclaw gateway restart` 进行。
- 设计与实现计划：`docs/superpowers/specs/2026-06-22-frontend-rebuild-design.md`、`docs/superpowers/plans/2026-06-22-frontend-rebuild-model-gateway.md`；架构记录见 `docs/前端功能架构.md`。
- Aurora 仍是视觉/IA 真相源：`docs/prototypes/app/styles.css` 提供 token、`docs/prototypes/pages/*.html` 提供页面 IA；本次把它们组件化落地到 shadcn，而不再 raw 渲染。
- Model Gateway IA is now locked in `docs/模型网关信息架构契约.md`: four primary tabs only, client App Connection remains a Model Gateway child flow, account pool is account-Provider-only, and dangerous writes must go through confirmation/evidence.
- Model Gateway detailed execution goal is now locked in `docs/模型网关详细目标.md`: overview must not create, Provider creation belongs to the Provider domain, Codex account login is separate from API Provider creation, and completion requires route/client/account/provider/model flows to pass desktop/mobile plus API verification.
- Model Gateway overview 围绕路由状态/路由检查/关注项重构（非 KPI 卡墙）；Provider 配置为子页分区流（基础/Endpoint/模型/高级）；Account Pool 仅账号制 Provider 显示，否则显式边界；Usage 不伪造行、空数据用空态。
- Workspace IDE 仍是核心长期域；目标见 `docs/工作区IDE目标.md`。
- Model Gateway detailed execution goal is now locked in `docs/模型网关详细目标.md`: overview must not create, Provider creation belongs to the Provider domain, Codex account login is separate from API Provider creation, and completion requires route/client/account/provider/model flows to pass desktop/mobile plus API verification.
- Model Gateway overview 围绕路由状态/路由检查/关注项重构（非 KPI 卡墙）；Provider 配置为子页分区流（基础/Endpoint/模型/高级）；Account Pool 仅账号制 Provider 显示，否则显式边界；Usage 不伪造行、空数据用空态。
- Workspace IDE 仍是核心长期域；目标见 `docs/工作区IDE目标.md`。

## 3. Retired Scope

Do not restore without a new PRD and user approval:

- Dreaming / memory management UI, BFF and shared types.
- Legacy plugin management UI, BFF and shared types.
- Old Gateway usage archive/pagination/CSV/cost/account/provider-key surface.
- Legacy platform action / third-party migration-first implementation tracks.

OpenClaw host/runtime plugin configuration and Recovery plugin-drift repair remain valid because they are runtime support, not legacy plugin management.

## 4. Open Work

P0:

- Keep Gateway stability closed: provider/protocol matrix, route health, unsupported envelopes and App Connections.
- Finish product docs reset and classify domains as `core`, `support`, `delegate-to-openclaw`, `retire-candidate` or `retired`.
- Tracevane has been adopted as the product name after the current preflight; keep registrar and trademark checks as a release gate before public launch.

P1:

- Graduate the remaining prototype-backed React page to real functionality, now focused on approvals. See `docs/前端功能架构.md`.
- Replace prototype-contract data with real TanStack Query aggregate APIs page by page, without changing backend until each API contract is written and verified.
- Convert Dashboard from mock summary into a task-first live operations cockpit.
- Turn Chat from prototype workbench into a real Agent operations workbench with sessions, evidence, artifacts and controls.
- Build Workspace IDE foundation beyond prototype: file explorer, multi-tab editor, terminal/task panel, Git summary, Markdown preview and diff review.
- Add live preview/evidence loop for HTML/static/local web apps, then preview-time editing through explicit source diffs.
- Add compact observability views for traces, tool calls, approvals, provider errors and token/cost summaries.
- Shrink Config/Agents/Channels/Skills to Tracevane-specific workflows and delegate generic OpenClaw CRUD.
- For every new product surface, update the benchmark decision record first: source checked, market lesson, Tracevane translation, rejected clone behavior and verification plan.

P2:

- macOS/Windows recovery portability validation.
- Broader provider/account proof after Gateway stability is closed.
- Public-release naming/legal preflight.

## 5. Recent Verification Baseline

> Rule: keep only the current baseline; per-round verification history lives in git history.

- Standard gate for code changes: `npm run typecheck:api`, `npm run typecheck:web`, `npm run build:api`, `npm run build:web`, plus targeted `node --test tests/system/...` and Playwright desktop/mobile smoke for the touched route.
- Frontend contract tests: `node --test tests/system/tracevane-react-aurora-frontend.test.mjs` and `tests/system/tracevane-domain-inventory.test.mjs`; coverage via `npm run tracevane:frontend-coverage` and `npm run tracevane:inventory`.
- Dreaming and legacy plugin management removal verified: `/api/plugins/summary` and `/api/system/dreaming` return 404.
- Rename closure verified: tracked scan for retired product id / route prefix / env prefix / gateway provider names returns no matches; `git diff --check` clean; release metadata is `@binbin/tracevane`, plugin id `tracevane`, OpenClaw min host `>=2026.4.8`.
- Live dev baseline: frontend `http://127.0.0.1:5176`, backend `http://127.0.0.1:3761`; `/api/system/health` version `0.1.70` with `gateway=online`; `/api/openclaw-recovery/status` `healthy`.

## 6. Current Development Rule

Every new Gateway, CLI Agent, provider, SDK/API, protocol, channel or user-visible workflow change must begin with current external research and end with local or live verification appropriate to the risk.
