# OpenClaw Studio 当前进展

> 状态：Active Snapshot
> 更新：2026-06-17
> 规则：只保留当前状态、验证和下一步；历史实现细节看 git。

## 1. 总体状态

OpenClaw Studio 已从早期恢复阶段进入真实工作台迭代：

- Config、Skills、Terminal、Channels、Cron、Agents、System 已具备真实读写和运行态能力。
- Channel Connectors 管理面已进入正式重构：`/channel-connectors` 主页面收敛为概览、渠道绑定、运行状态、会话日志四个同级工作区，不再内嵌 CLI Profile 快改或 Skills 管理；Codex / Claude Code / OpenCode 的 Profile、模型、上下文预算、工作目录、权限、IM 绑定、持久会话和事件记录由 `/channel-connectors/profiles` 统一管理，不挂在 OpenClaw Agents 子页。
- Chat 已进入运行态，继续保持 Studio typed adapter / BFF 边界。
- Studio Gateway 已成为正式模型中转域，已停止演进的旧模型链路不再作为用户路径。
- Channel Connectors 已进入 Studio 原生 daemon 路线，当前聚焦 Feishu/Octo 私聊 + CLI Agent Bot。
- Dashboard、Files、Plugins、Dreaming 仍是持续打磨域。
- Room / Workflow 暂缓。

## 2. 当前主线

### Studio Gateway

- 独立 Gateway daemon、Provider Center、App Connections、active routing、模型目录、模型能力和上下文预算已形成主线。
- Gateway 目标是任意 provider 原生协议都能对外暴露 Anthropic Messages、OpenAI Responses / compact、OpenAI Chat Completions。
- Gateway 协议转换和新 provider 能力必须先完成官方/API/SDK/GitHub/社区核验，并用本地回归或 live smoke 证明；无法验证的组合保持结构化 unsupported。
- 真实 provider smoke 已覆盖 BigModel Chat、BigModel Anthropic、GMN Responses-native substitute；OpenAI Platform 官方端点保留为后续可选 vendor proof。
- 本地 BigModel/GLM provider 已加入 `glm-5.2`，按 1M context / 128K output 预算；Gateway 内置推断覆盖 `glm-5.2` 和 `glm-5.2[1m]`。
- Provider 已支持 endpoint profiles；本地 `glm` provider 配置 BigModel Coding Chat 与 Coding Anthropic 两个端点，同一模型会按客户端协议优选原生 endpoint，并按 endpoint health/circuit 回退；Provider Center 已支持 endpoint profile 编辑、detect 同步和端点级 smoke。
- Provider Center 已收口为任务分区式配置：基础连接、端点路由、密钥识别、模型目录、高级覆盖、可用范围；模型目录只暴露“模型名称”和“别名”两个身份字段，PC/平板/手机均已做响应式验证。
- Model Gateway 支持 `tab/app` deep-link；Channel Connectors Profile 工作台可从当前 CLI Profile 直达对应 Codex / Claude Code / OpenCode App Connection 并高亮。

### Channel Connectors

- Studio 原生 Channel daemon 已可配置、启动、守护和查看运行态。
- Feishu/Octo 私聊文本、文件、图片、工具流、核心命令已可用；过程回复 parser 已接入，Codex / Claude Code / OpenCode 均有真实 IM live 证据。
- 新增 `scripts/smoke-channel-connectors-agent-runner-direct.mjs`，用于分层验证真实 CLI runner/parser，不注入 IM 事件；三 Agent direct smoke 已通过。
- IM busy guard 已取代普通消息排队：Feishu/Octo 同 binding + IM session 已有任务时，后续普通消息直接提示 `/stop`/`/cancel` 后重发，不入队、不落 pending store、不 daemon 重启 replay。
- Feishu 命令/菜单按当前 IM 命令合同维护：执行时给触发消息加 reaction，卡片/文本回复挂回原消息；`/compact` 和 `/native /compact` 会发 started/terminal progress。
- Feishu Agent 进度卡动态条数已可在 `/display progress <1-30|default>` 和进度显示卡片内设置，写入 binding metadata `feishuProgressCardEntryLimit`，运行时按 1-30 裁剪。
- Feishu 卡片菜单已收敛为清爽二级配置面板：主卡直达 Agent、模型、权限/推理、进度显示、视觉、工作目录六个配置页；工作目录页支持 Profile 默认、上一目录、上级目录、Home、最近目录直达列表、子目录分页直达列表和 `/dir find` 搜索；纯文本渠道支持 `/dir home|parent|recent N|child N`；文本 `/help` 与卡片菜单共用同一信息架构。
- Feishu/命令模型菜单改为读取 Gateway 聚合模型列表，不再 12 条截断；模型/视觉选择器会显示已展示/总数，超过卡片上限时仍可用 `/model <模型ID>` 手动切换。
- Channel Connectors Profile 前端已重排为独立工作台：顶部摘要条展示当前 Profile、effective model、绑定数、活动会话和编辑状态；左侧 Profile/IM 绑定，中间运行配置，右侧会话与事件已拆为持久请求、活动会话、事件日志三段，session/event trace 默认折叠；IM 绑定摘要展示启停、连接健康和 session driver requested/effective mode；active session 行展示 profile/session/pool/turns/idle trace；session event 行展示 agent/model/session/message/workdir trace，失败事件使用 danger 标记，binding 行可一键过滤右侧事件，事件列表支持按 binding/type 筛选和 8/20/50 显示数量；模型下拉读取 Studio Gateway 可用模型目录并合并启用 provider 的 alias，模型与上下文分区会展示上下文窗口、输出预算、auto compact 阈值和预算来源；CLI App Connection 已拆成独立配置区；effective model 按 `Profile model > app-specific model > Gateway default model` 解析，Profile 列表同步显示继承后的模型，并展示 CLI App Connection 协议、endpoint、配置状态、目标配置文件、最近备份、启动提示、阻断原因和脱敏 preview，可直接应用当前 Profile 到对应 CLI App；Profile 复制/删除保护、事件筛选、批量停止活动 session、模型网关 App Connection deep-link、IM binding/requested persistent binding/session/event 直达配置、主配置页返回 Profile 工作台保留当前 `profileId`、未保存撤销和 ID 重命名绑定迁移已补；旧 `/agents/:agentId/cli` 已删除。
- Channel Connectors Profile 真实闭环继续推进：切换 CLI Agent 会清理继承来的 stale `App Profile` 引用，新建 Profile 默认使用 `default` App Profile；从 Profile 页“应用到 CLI”不会再把当前 Profile 模型写入 Gateway App Connections 的全局默认 `profile.model`，只更新对应 CLI 的 app-specific model。
- Channel Connectors 主配置页重构继续推进：移除左侧 daemon 侧栏、内嵌 Profile 快改和 Skills 管理；新增运营概览，保留 daemon service 操作、Profile 工作台入口、绑定摘要和 busy guard 状态入口；`bindingId/profileId` deep-link 会进入 Bindings 并选中对应 binding；Bindings 已拆为左侧绑定列表和右侧分区编辑器；Runtime/Sessions 已拆为运行摘要、链路、busy guard、会话和事件工作区，原始日志默认折叠；移动端操作区和 Tab 已做纵向/两列降级。
- Feishu card action 已改为同步 callback response 返回新卡片/toast，普通消息和 bot menu 仍保持后台异步，避免卡片长时间未点后出现 108002 类不可用体验。
- active `studio-channel-skill` / platform action 方向已删除；平台 API 工具层不再扩展。
- Feishu transport 未暴露的 Docx / Drive / Wiki / Bitable 直接 action helper 已删除；私聊消息、文件/图片、reaction、成员/线程读取主链路保留。
- Codex 隔离 `codex-home/skills` 会清理历史生成的 Feishu/Octo platform action skill，当前运行态旧目录已删除，避免 stale skill YAML 再污染 Agent 会话。
- 群聊、thread、多 bot 协作和管理类 Octo/Feishu action 只保留已实现 best-effort，不再作为当前主线。
- 上下文预算核心已完成：`/status` 展示 Gateway 模型窗口、输出预留、usage/history estimate、剩余窗口和 auto compact threshold；auto compact 走 Agent-native first，失败才 fallback 到 Studio/Gateway compact。
- Claude Code / OpenCode persistent native compact 已有 driver 回归；Feishu 显式 `/compact` 24h live 已验证 Codex / Claude Code / OpenCode 进入 Agent-native compact；Octo auto compact 和显式 `/compact` 24h live 已验证；Codex / Claude / OpenCode 结构化与混合 content 工具输出、per-agent live `--require-tool-output`、Codex reasoning summary 解析和 Octo `/thinking` 开关回归已补。
- 本轮：IM Agent 默认 session driver 已从 one-shot/TUI 兼容 runner 切到结构化 persistent；Codex 走 app-server，Claude Code 走 stream-json 常驻进程，OpenCode 走 `run --session`。显式 metadata 仍可 opt-out 到 one-shot，persistent 创建/执行失败会写 `turn.failed` / `turn.fallback` 并按命令策略降级。
- 本轮修复 Feishu 显式 `/compact` 并发干扰：persistent session pool 不再因同 session busy turn dispose 活跃 session；native compact 禁止回退不支持的 one-shot；显式 `/compact` 使用同 session guard，busy 时提示先 `/stop`/`/cancel` 或等待完成，不再排队。
- Feishu 入站文件 live 已验证：文件消息 staging、history 附件摘要、Agent 读取文件和回复均通过。
- Live 附件验收脚本已加固：`--require-inbound-image`、`--require-inbound-video`、`--require-staged-files` 会验证附件类型和本地 staged 路径存在；Feishu/Octo 入站文件、Feishu/Octo 入站图片和 Feishu 入站视频已通过只读日志 smoke。
- 私聊文件/消息收发核心已完成：入站 staging、出站 file/message manifest、原始文件名、Feishu/Octo 上传发送、Octo COS/STS 大文件路径和 fallback 均有回归；后续只抽查平台大小限制、真实大文件和异常路径。
- Feishu 出站文件和权限审批 24h live 已通过；Feishu 审批真实形态是进度卡片 `permission-pending/allowed` + 卡片按钮 `channel.command commandAction=permission commandOk=true`。Octo 入站文件 24h live 已通过并能返回 staged 路径；Octo `.mp4` 文件形态视频 24h smoke 已通过；Octo 出站文件 24h live 自动事件证据已通过。
- 用户确认 Feishu/Octo 最新手动验收全部通过，包括 Feishu 发文件、权限审批、Octo 收文件并返回路径；`hello-live.txt` 和 `studio-greeting.txt` 临时文件已删除。
- 图片自动切视觉模型默认关闭，需在平台 binding 或 IM `/vision` 显式开启；binding 可设置默认 fallback 模型，IM 会话可通过 `/vision` 命令和 Feishu 菜单临时开启、关闭、指定模型或回到 binding 默认。开启后失败会回退原模型的附件说明/本地路径模式。
- Codex 图片异常排查结果：当前 Feishu 私聊 override 实际为 `gpt-5.4-mini`；已修正 Codex `exec resume` 图片 option 顺序，并让 Codex app-server result 保留视觉 args，便于日志确认图片是否进入模型。
- Gateway 已修复 Responses -> Chat-compatible provider 的 `input_image` 映射；`gpt-5.4-mini` / `gpt-5.5` 受控图片 smoke 和 `codex exec --image` + `gpt-5.4-mini` 已通过。当前没有 `gpt-5.5-mini` 模型；`claude-opus-4-6` 走 `mlamp` Chat-compatible 图片请求仍返回 400，需按 provider smoke 结果标记能力。
- Provider Center 已停止按模型名推断 vision；`gpt-*`、`claude-*` 这类名称只有在用户勾选、上游显式能力元数据或图片 smoke 通过后确认时才会进入图片能力。Smoke 页已提供独立图片 smoke，失败只提示协议/endpoint/模型不匹配，不写回 vision。
- Codex / Claude Code / OpenCode 图片链路已用受控三色方块真实 runner smoke 验证通过；非视觉模型会收到附件说明和本地路径，不传 native 图片、不允许猜图。
- 视频附件不再由 Studio 预抽帧或转图片；Studio 只负责接收、staging 和把本地路径交给 Agent，由 Agent CLI/模型或工具按任务自行处理视频。
- Octo 媒体消息带 `content/caption` 时会保留用户任务文本，不再被 `[image]` / `[video]` 占位吞掉。
- 同 session FIFO/pending replay 已废弃：pending store 只用于启动时清空遗留记录；普通 IM busy 消息直接拒绝，daemon/API/UI 展示 legacy pending 清理状态。
- Codex app-server persistent turn 超时已改为分层 idle：普通静默默认 3 分钟；审批请求会刷新 idle，等待 IM 审批按审批窗口处理，批准工具后才给长工具执行窗口；fallback 恢复型 `turn/timeout` 不进入 Feishu/Octo 用户进度流，Feishu 进度卡终态只由最终 Agent run 结果决定。
- Codex / Claude Code / OpenCode native CLI runner 已改为 CLI 心跳超时：stdout/stderr 中的 `Working (... esc to interrupt)`、`Imagining...` 等状态会续期等待，只有 CLI 停止输出心跳才返回 `process/heartbeat-timeout`，避免 Feishu/Octo IM 长任务仍在工作时被固定总时长误杀；持续只有 TUI 心跳但没有结构化进展时会记录 `process/heartbeat-stall` 非终态诊断，该诊断不刷新 timeout、重复诊断退避节流、默认不推送到 IM 过程消息；权威失败终态覆盖 exit 0，权威完成终态后 lingering CLI grace 收尾；本轮先不跑渠道 live，已新增本地合成矩阵覆盖 stdout、stderr CR-only TUI、heartbeat-only stall、idleTimeout 替代总超时、静默 timeout 和非 runtime Agent 固定超时。
- 真实 CLI thinking smoke 已完成：Claude Code 2.1.86 当前不输出 `thinking` item；OpenCode 1.17.0 在支持 reasoning 的模型上可输出 `reasoning` part；状态、菜单和前端已区分 parser/live 支持。
- 下一步聚焦收口而不是扩面：Channel Connectors 只保留回归抽查；新开发优先转向 Studio Gateway 可选官方 OpenAI proof、更多账户型 provider，以及 Chat typed contract 体验/性能收口。

### Chat

- Chat 仍是 Studio 自管会话工作台，不直接暴露 Gateway raw frame。
- 后端 typed contract、会话列表、history、send、abort、reset、upload、stream 和 rich message 渲染已进入主线。
- 前端主实现集中在 `apps/web-vue/src/features/chat/**`。
- Chat 文档已压缩为合同和策略参考，避免继续追加性能流水账。

## 3. 当前验证基线

最近完整验证来自 Channel Connectors 任务：

- 本轮：`npm run typecheck:api`
- 本轮：`npm run typecheck:web`
- 本轮：`npm run build:api`
- 本轮：`npm run build:web`
- 本轮：`node --test tests/system/studio-web-channel-connector-profiles-page.test.mjs`
- 本轮：`node --test tests/system/model-gateway-service.test.mjs`，57/57 通过，覆盖 `glm-5.2` / `glm-5.2[1m]` 预算推断、endpoint profile 原生协议优选、endpoint health 回退、响应头和 endpoint 级 smoke
- 本轮：`node --test tests/system/studio-web-model-gateway-page.test.mjs tests/system/studio-web-channel-connector-profiles-page.test.mjs`，覆盖 Provider Center 分区布局、模型名称/别名二段式配置和 Channel Connectors Profile 模型目录读取
- 本轮：`node --test tests/system/studio-web-channel-connector-profiles-page.test.mjs tests/system/studio-web-channel-connectors-page.test.mjs`，覆盖 Channel Connectors Profile 三栏布局、Gateway 预算索引、auto compact 展示、Profile 复制/删除/binding 行事件快捷过滤/事件 binding/type 筛选/事件数量/批量停止控件、Profile ID 重命名迁移绑定合同、App Connection effective model / apply / preview 合同、IM binding deep-link 选中合同、Agents 旧 CLI 路由删除和 Channel Connectors 独立导航
- 本轮：`node --test tests/system/studio-web-channel-connectors-page.test.mjs tests/system/studio-web-channel-connector-profiles-page.test.mjs`，5/5 通过，覆盖 Channel Connectors 主页面四区结构、旧 Profile 快改/Skills 管理入口移除和 Profile 独立工作台入口
- 本轮：`node --test tests/system/studio-web-channel-connector-profiles-page.test.mjs` 通过，覆盖 Profile 页 Agent 切换清理 stale App Profile ref、新建 Profile 默认 `default` App Profile、Profile Apply-to-CLI 不改 Gateway 全局默认模型
- 本轮：Python Playwright 真实交互通过，`/channel-connectors/profiles?profileId=claude` 点击“应用到 CLI”，确认 `profile.model` 保持 `null`、`appModels["claude-code"]` 被应用、Claude Code rollback 成功，并恢复 App Connection profile 原值
- 本轮：`node scripts/smoke-channel-connectors-agent-sessions.mjs --json` 通过，daemon session 管理 endpoint reachable，Feishu/Octo binding 均为 effective persistent，当前 active session 为 0
- 本轮：`node scripts/smoke-channel-connectors-native-cli-sessions.mjs --apps claude-code,opencode --json` 通过，isolated real CLI session 覆盖 Claude Code / OpenCode normal turn、file manifest、native visual input、native compact 和 stop/cancel，不污染真实 HOME/runtime
- 本轮：`node scripts/smoke-channel-connectors-command-live.mjs --recent-sessions --probe --commands /status,/model,/mode,/dir,/compact --json` 通过，Feishu/Octo 最近 session 可 dry-run 解析模型、权限、工作目录和 compact 命令；probe 不发送平台消息、不修改状态，不替代真实 IM live
- 本轮：`node scripts/smoke-channel-connectors-feishu-long-connection.mjs --json` 通过，70 秒采样内 Feishu 长连接 connected/sdkConnected，ping/pong 正常，`transportStale=false`，`violations=0`；runtime 同时显示 Octo connection 为 1、legacy pending 为 0
- 本轮：新增 `node scripts/smoke-channel-connectors-profile-closure.mjs --json` 闭环验收入口，统一检查三 Agent live run、Feishu 显式 `/compact`、Octo 显式 `/compact`、入站图片 staged-path 四个真实 IM gate；`--plan --json` 可输出缺口触发口径
- 本轮：`npm run typecheck:api`
- 本轮：`npm run build:api`
- 本轮：`node --test tests/system/channel-connectors-agent-session-driver.test.mjs tests/system/channel-connectors-compact-live-script.test.mjs tests/system/channel-connectors-profile-closure-script.test.mjs`，19/19 通过，覆盖 persistent session busy guard、不 dispose 活跃 session、native compact 禁用 one-shot crash fallback、Claude/OpenCode compact driver 和 closure gate 脚本合同
- 本轮：`node --test tests/system/channel-connectors-agent-session-driver.test.mjs`，16/16 通过；`node --test tests/system/channel-connectors-service.test.mjs`，124/124 通过；`node --test tests/system/channel-connectors-agent-sessions-live-script.test.mjs`，3/3 通过，覆盖默认 persistent、显式 one-shot opt-out、persistent 创建失败 fallback、Codex app-server 默认路由、Claude/OpenCode persistent compact、busy/delivery guard 和 session status smoke
- 本轮：已重启 `openclaw-studio-channel-connectors.service`；`/agent-sessions` 显示 `defaultMode=persistent`，当前 Feishu/Octo live binding 均为 `requestedMode/effectiveMode=persistent`、`reason=codex-app-server`；`/status` 显示 Feishu connected/sdkConnected 且 `transportStale=false`，Octo connected
- 本轮：`node scripts/smoke-channel-connectors-profile-closure.mjs --json` 通过，`three-agent-live-run`、`feishu-explicit-compact`、`octo-explicit-compact`、`inbound-image` 四个真实 IM gate 全绿
- 本轮：Headless Chrome 打开 `/channel-connectors` 生成桌面/窄屏截图，CDP 验证 `documentElement.scrollWidth === clientWidth`，主页面无水平滚动
- 本轮：Headless Chrome 打开 `/channel-connectors?bindingId=feishu-live&profileId=feishu-codex` 生成桌面/390px 截图，CDP 验证 `documentElement.scrollWidth === clientWidth`，Bindings 列表 + 分区编辑器无水平滚动
- 本轮：Headless Chrome 打开 `/channel-connectors` 并切换 Runtime / Sessions，桌面/390px 截图，CDP 验证 `documentElement.scrollWidth === clientWidth` 且可见元素无溢出
- 本轮：Headless Chrome 打开 `/channel-connectors/profiles?profileId=feishu-codex`，桌面/900px/390px 截图，CDP 验证 `documentElement.scrollWidth === clientWidth` 且可见元素无溢出；移动端 Header 不再挤窄
- 本轮：Headless Chrome 打开 `/channel-connectors/profiles?profileId=feishu-codex` 并定位 Sessions 区，桌面/900px/390px 均无横向溢出；事件 trace 默认折叠，请求行文本不再粘连
- 本轮：Python Playwright 打开 `http://127.0.0.1:5176/channel-connectors/profiles`，1440/900/390 宽度下无横向溢出，Profile 复制/删除、模型网关、停止全部和事件筛选控件均渲染；旧 `http://127.0.0.1:5176/agents/main/cli` 不再渲染 CLI Profile 管理
- 本轮：浏览器验证 Profile 页当前有 2 个 IM binding 配置入口；`/channel-connectors?bindingId=feishu-live&profileId=feishu-codex` 会进入 Bindings 工作区并选中 `Feishu Live` / `Feishu Codex`，无横向溢出
- 本轮：Python Playwright 验证从 `/channel-connectors?profileId=feishu-codex` 点击 Profile 工作台会进入 `/channel-connectors/profiles?profileId=feishu-codex`，并选中 `Feishu Codex`
- 本轮：Python Playwright 验证 `/channel-connectors/profiles?profileId=feishu-codex` 当前 8 条 session event 均渲染“绑定”入口和 agent/model/session/message/workdir trace；当前 live 样本 active session 为 0，桌面/手机无横向溢出；active session trace 与失败态由源码测试覆盖
- 本轮：Python Playwright 验证 `/channel-connectors/profiles?profileId=feishu-codex` 当前 2 条 requested persistent binding 均渲染“绑定”入口，桌面/手机无横向溢出
- 本轮：Python Playwright 验证 `/channel-connectors/profiles?profileId=feishu-codex` 当前 2 条 IM binding 摘要均展示 session driver mode/reason，桌面/手机无横向溢出
- 本轮：Python Playwright 验证 `/channel-connectors/profiles?profileId=feishu-codex` 的 CLI App Connection 卡片显示配置文件、最近备份、启动提示、脱敏 preview、App 连接入口和当前 CLI apply 入口，桌面/手机无横向溢出
- 本轮：Python Playwright 验证 `/channel-connectors/profiles?profileId=feishu-codex` 的 Profile 列表显示 resolved model 标签，当前样本为 `glm-5`，桌面/手机无横向溢出
- 本轮：Python Playwright 验证 `/model-gateway?tab=connections&app=codex` 会打开 App Connections，并在桌面/手机高亮 `Codex CLI` 卡片，无横向溢出
- 本轮：Python Playwright 验证 Profile 页面在 1440/900/390 宽度下渲染 CLI App connection 与 App Profile 受控选择，无横向溢出
- 本轮：Python Playwright 无保存交互验证通过，Profile ID 修改会显示未保存状态，撤销可还原；输入已有 ID 会显示冲突并禁用保存
- 本轮：已重启 dev 前后端；前端 5176、后端 3761，Gateway daemon 与 Channel daemon 均为 `active/enabled`
- 本轮：Python Playwright 打开 `http://127.0.0.1:5176/model-gateway`，在 1440/900/390 宽度下进入 Provider configuration，无页面横向溢出；手机端模型行字段标签可见
- 本轮：本机 Gateway `glm-5.2` 三协议 live smoke 通过，Chat / Anthropic / Responses 入口分别命中 `coding-chat` / `coding-anthropic` / `coding-chat`
- 本轮：本机 Provider Center endpoint smoke API 指定 `glm/coding-chat` 和 `glm/coding-anthropic` 均通过
- 本轮：`node --test tests/system/channel-connectors-service.test.mjs`，104/104 通过，覆盖 Gateway 模型列表超过 12 个时 Feishu/命令模型菜单仍显示后续模型
- `npm run typecheck:api`
- `npm run build:api`
- `npm run typecheck:web`
- `npm run build:web`
- `node --test --test-name-pattern "IM commands switch agent|visual turns select Gateway vision models|model menus can read live Gateway model lists|command surface renders text" tests/system/channel-connectors-service.test.mjs`，4/4 通过
- `node --test --test-name-pattern "rejects same-session|does not persist or replay busy Octo Agent turns" tests/system/channel-connectors-service.test.mjs`，覆盖 busy guard、第二条不进 runner、pending store 为空、重启不 replay
- `node --test tests/system/channel-connectors-feishu-durable-queue-live-script.test.mjs`，5/5 通过
- `node --test --test-name-pattern "does not persist or replay busy Octo Agent turns|daemon keeps Feishu dispatcher parity diagnostics" tests/system/channel-connectors-service.test.mjs`，2/2 通过
- `node --test --test-name-pattern "model gateway adapts non-streaming codex responses requests to openai chat providers" tests/system/model-gateway-service.test.mjs`
- `node --test --test-name-pattern "Octo adapter dry-run dispatch resolves binding, session key, and reply plan" tests/system/channel-connectors-service.test.mjs`
- `node --test --test-name-pattern "native Channel Connectors agent runner builds gateway-backed Codex turns" tests/system/channel-connectors-service.test.mjs`
- `node --test --test-name-pattern "image" tests/system/channel-connectors-codex-app-server-driver.test.mjs`
- `node --test --test-name-pattern "mixed content tool output" tests/system/channel-connectors-service.test.mjs`
- `node --test --test-name-pattern "(structured tool output|mixed content tool output|final text|JSON progress|agent messages before later tools|stream-json progress|text before later tools|DB fallback keeps tool results|persistent Claude driver keeps intermediate)" tests/system/channel-connectors-service.test.mjs`，10/10 通过
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 720 --agents codex,claude-code,opencode --require-agent-coverage --require-ok --require-reply --require-tool --require-tool-output --min-runs 3 --limit-runs 12`，近 12h 三个 Agent 均有成功工具调用和可见工具输出证据
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --agents codex,claude-code,opencode --require-agent-coverage --require-ok --require-reply --require-tool --require-tool-output --require-process-reply --min-runs 3 --limit-runs 0 --json`，通过，`matchingAgents=3`、`missingAgents=0`、`requirementViolations=0`
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --wait --timeout-ms 600000 --poll-ms 1000 --agents opencode --require-agent-coverage --require-ok --require-reply --require-tool --require-tool-output --require-process-reply --min-runs 1 --json`，通过，匹配 Feishu OpenCode 真实 IM run：`assistantIntermediateProgressCount=3`、`toolOutputSignalCount=3`、`latestFeishuProgressCardStatus=completed`
- `node scripts/smoke-channel-connectors-agent-runner-direct.mjs --agents codex,claude-code,opencode --json`，三 Agent 均通过真实 Gateway-backed direct runner smoke；每个 Agent 都有 3 条过程回复、3 个可见工具结果和 1 条最终回复
- `node scripts/smoke-channel-connectors-agent-runner-direct.mjs --json`，默认 OpenCode direct runner smoke 通过
- `node --test tests/system/channel-connectors-agent-runner-direct-script.test.mjs`，2/2 通过，锁定 direct runner smoke 是 parser-only proof，不替代 Feishu/Octo event-log 证据
- direct runner smoke 默认使用 `/tmp` 隔离 CLI runtime 并自动清理；旧 `direct-runner-smoke` 运行目录已删除。
- `node --test tests/system/channel-connectors-agent-run-live-script.test.mjs tests/system/channel-connectors-feishu-durable-queue-live-script.test.mjs`，覆盖 OpenCode process-reply 等待入口和 Feishu busy guard 证据脚本
- `node --test tests/system/channel-connectors-agent-run-live-script.test.mjs`，12/12 通过，覆盖 `--agents`、`--require-agent-coverage`、`--require-process-reply` warning 诊断、`--limit-runs 0` 输出压缩、`--require-tool-output`、`--require-stop-command`、入站图片/视频/文件 staged local path、Octo `.mp4` 作为 `file` 的视频识别、Feishu 卡片审批 command 形态，以及 human 输出只展示匹配 run
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --require-ok --require-reply --require-inbound-file --require-staged-files --min-runs 1 --limit-runs 3 --json`，匹配 Feishu/Octo 入站文件且 staged 路径存在
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --require-ok --require-reply --require-inbound-image --require-staged-files --min-runs 1 --limit-runs 3 --json`，匹配 Feishu/Octo 入站图片且 staged 路径存在
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --require-ok --require-reply --require-inbound-video --require-staged-files --min-runs 1 --limit-runs 3 --json`，匹配 Feishu 入站视频且 staged 路径存在
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms octo --require-ok --require-reply --require-inbound-video --require-staged-files --min-runs 1 --limit-runs 3 --json`，匹配 Octo `.mp4` 以 `file` 形态入站且 staged 路径存在
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms feishu --require-ok --require-reply --require-file --min-runs 1 --limit-runs 3`，匹配 Feishu 出站文件 24h 成功样本
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms feishu --require-ok --require-permission-prompt --require-permission-resolved --require-feishu-permission-progress-card --min-runs 1 --limit-runs 3`，匹配 4 条 Feishu 卡片权限审批成功样本
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms octo --require-ok --require-reply --require-inbound-file --require-staged-files --min-runs 1 --limit-runs 3`，匹配 Octo 入站文件 staged 路径存在样本
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms octo --require-ok --require-reply --require-file --min-runs 1 --limit-runs 5 --json`，匹配 Octo 出站文件成功样本，`outboundFilesSent=1`
- `node --test tests/system/channel-connectors-compact-live-script.test.mjs`，4/4 通过
- `node scripts/smoke-channel-connectors-compact-live.mjs --platform feishu --mode explicit --since-minutes 1440 --json`，识别 Feishu 24h 三 Agent 显式 `/compact` native 证据
- `node scripts/smoke-channel-connectors-compact-live.mjs --platform octo --mode auto --since-minutes 1440 --json`，识别 Octo 24h auto compact native 证据
- `node scripts/smoke-channel-connectors-compact-live.mjs --platform octo --mode explicit --since-minutes 1440 --json`，识别 Octo 24h 内显式 `/compact` native 证据
- `node --test tests/system/channel-connectors-feishu-durable-queue-live-script.test.mjs`，5/5 通过
- `node scripts/smoke-channel-connectors-feishu-durable-queue-live.mjs --wait --json`，legacy 脚本名下检查 Feishu long-connection busy guard：期望 `channel.agent.rejected_busy` 且被拒消息没有 `agent.run.started`
- `node --test --test-name-pattern "IM commands switch|Feishu transport can reply|Feishu command replies use progress reactions|daemon keeps Feishu compact native-first" tests/system/channel-connectors-service.test.mjs`，4/4 通过，覆盖 Feishu reply-to、命令 reaction 和 compact command progress。
- `node --test tests/system/channel-connectors-command-live-script.test.mjs`，8/8 通过，覆盖命令进度日志和 Feishu command progress card patch。
- `node --test --test-name-pattern "progress|Channel Connectors page calls" tests/system/channel-connectors-service.test.mjs tests/system/studio-web-channel-connectors-page.test.mjs`，13/13 通过，覆盖 Feishu 进度卡条数配置和前端保存字段。
- `node --test --test-name-pattern "command surface renders text and Feishu card actions" tests/system/channel-connectors-service.test.mjs`，通过，覆盖 Feishu 主卡/更多页直达真实配置页、子页移除 `/help xxx` 中间层、配置页去重复列表、权限/推理页分区、进度显示页分区、视觉设置页分区、会话页去低频按钮、会话按钮中文化、压缩文案统一、危险动作置底、视觉/显示单按钮切换、文本 `/help` 同步结构和长连接 card action callback response。
- `node --test --test-name-pattern "IM commands switch|slash compact works|Feishu command replies use progress reactions|daemon keeps Feishu compact native-first" tests/system/channel-connectors-service.test.mjs`，4/4 通过。
- `node --test --test-name-pattern "IM commands switch|command surface renders text and Feishu card actions" tests/system/channel-connectors-service.test.mjs`，2/2 通过，覆盖 `/help` 文本和 Feishu 主卡清爽二级配置结构。
- `node --test tests/system/channel-connectors-command-live-script.test.mjs`，8/8 通过。
- 真实 smoke：`/v1/responses` + `gpt-5.4-mini` / `gpt-5.5` 受控三色方块图片识别；`codex exec --image` + `gpt-5.4-mini` 识别同图成功。
- 真实 runner smoke：Codex / Claude Code / OpenCode + `gpt-5.4-mini` 识别受控三色方块图片成功；非视觉 `glm-5` 不传 `--image` 且按附件说明退回；视频附件合同验证为本地文件路径进入 Agent，不做 Studio 预抽帧。
- `node --test tests/system/channel-connectors-service.test.mjs`，104/104 通过，覆盖 Feishu 进度卡条数卡片内设置、工作目录快捷切换、子目录分页和 `/dir find` 搜索。
- `node --test tests/system/channel-connectors-codex-app-server-driver.test.mjs`，17/17 通过，覆盖 app-server 空闲超时、审批/批准工具刷新 idle、fallback 恢复 timeout 不进入用户进度和真正卡死 interrupt
- `node scripts/smoke-channel-connectors-agent-heartbeat-local.mjs --json` 与 `npm run smoke:channel-connectors:agent-heartbeat-local -- --json` 均 16/16 通过，覆盖三 Agent stderr CR-only TUI、stdout 心跳、heartbeat-only stall 诊断、idleTimeout 替代总超时、静默 heartbeat timeout 和非 runtime Agent 固定超时
- `node --test tests/system/channel-connectors-agent-heartbeat-local-script.test.mjs`，2/2 通过，覆盖本地 heartbeat smoke 帮助文本和完整矩阵
- `node --test --test-name-pattern "native Channel Connectors process runner" tests/system/channel-connectors-service.test.mjs`，29/29 通过，覆盖 Codex / Claude Code / OpenCode TUI 心跳续期、stdout 心跳、CR-only TUI 刷新、heartbeat-only stall 诊断、stall 诊断不刷新 timeout、重复 stall 诊断退避节流、权威失败终态覆盖 exit 0、权威完成终态后 lingering CLI grace 收尾、idleTimeout 替代总超时、静默 heartbeat timeout、最后活动流诊断、非 runtime Agent 固定 timeout、三 Agent 工具流和权限处理
- `node --test tests/system/model-gateway-service.test.mjs`，54/54 通过，覆盖名称-only 模型不自动 vision、显式图片元数据保留和图片 smoke 不污染 provider health/circuit
- `node --test tests/system/channel-connectors-persistent-live-script.test.mjs`，1/1 通过
- `node --test --test-name-pattern "stops Codex app-server persistent turns|Agent process cancelled|native compact" tests/system/channel-connectors-service.test.mjs`，2/2 通过，覆盖 Codex app-server persistent `/stop`
- `node scripts/smoke-channel-connectors-agent-run-live.mjs --since-minutes 1440 --platforms octo --require-stop-command --min-runs 1 --limit-runs 5 --json`，匹配 Octo `/stop` 命令和同 session cancelled run，命令与 run 不同 messageId 也能关联
- `node --test --test-name-pattern "native Channel Connectors agent runner builds gateway-backed Codex turns" tests/system/channel-connectors-service.test.mjs`，覆盖持久 `codex-home/skills` 旧 Feishu/Octo platform action skill 清理，普通自定义 skill 保留

本轮涉及 Channel Connectors 运行代码，完成后以 API typecheck/build 和相关系统测试为准。

## 4. 仍需推进

1. Studio Gateway：继续做可选 OpenAI Platform 官方端点 proof、更多账户型 provider、账户/普通 provider 统一消耗页细化和媒体模型 smoke。
2. Chat：继续按 typed contract 做体验与性能收口，不引入平行会话数据库。
3. Channel Connectors：主链路进入 monitored/回归抽查；只在真实 IM、CLI 或 smoke 暴露新事件形态时补 parser/driver，不再扩展群聊/platform action。
