# 文档整理记录

> 更新：2026-06-25

## 本轮目标

按 Workspace-first 方向重新整理 Tracevane 文档，解决旧版推翻后新前端 IDE / 文件管理 / 终端目标不清、半成品说明暴露、CLI Agent 与 Terminal 边界混淆的问题。

## 新增权威文档

- `整体目标.md`
- `Workspace目标.md`
- `Workspace设计文档.md`
- `Workspace架构.md`
- `Workspace前端架构.md`
- `Workspace后端设计.md`
- `文件管理设计.md`
- `终端能力设计.md`

## 被替代的旧方向

- 旧“三域重构”不再作为最高产品叙事；Model Gateway / IM Channels / CLI Agents 仍是重要域，但 Workspace 成为本地工作核心。
- 旧“工作区 IDE”文档中“IDE 是核心”的判断保留，但实现目标升级为 Workspace，而不是 VS Code 壳。
- 旧 Files 只读证据方向不再作为文件管理目标；如保留 `/files`，应改成 Evidence/Artifacts。

## 删除策略

删除只针对已经造成方向冲突、且有新文档替代的旧阶段性计划。仍有领域价值的文档保留并降级为参考。
## 2026-06-25 Workspace 前端 IDE / 文件管理重构 Phase 0

新增 `Workspace前端IDE文件管理重构阶段目标.md`，作为后续前端重构的执行合同。该文档明确：Terminal 属于 Workspace，CLI Agents 不管理普通终端；文件管理必须进入 Workspace 真闭环；PC/Tablet/Mobile 使用不同布局形态；每个阶段完成后必须更新文档、验证并提交 git。
